[client]
# Show the default Streamlit toolbar (auto = show on hover, developer = always show)
toolbarMode = "auto"

# Hide the default sidebar navigation (we're using custom collapsible navigation)
showSidebarNavigation = false

[ui]
# Hide the default "Deploy" button
hideTopBar = false

[theme]
# Optional: Customize theme colors for dark mode
primaryColor = "#FF8C00"
backgroundColor = "#0E1117"
secondaryBackgroundColor = "#262730"
textColor = "#FAFAFA"
font = "sans serif"
